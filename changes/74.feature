Add `robots.txt` file with rules to:

- optimize crawler search efficiency and safeguard server resources
- apply restrictions to block crawlers from deep paging and sorting (`/*?*page=` and `/*?*sort=`).
